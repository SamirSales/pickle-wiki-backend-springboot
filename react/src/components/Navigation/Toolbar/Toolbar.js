import React from 'react';

import './Toolbar.css';

const toolbar = ( props ) => {

    return (
        <header className="toolbar">
            <div>MENU   </div>
            <div>LOGO</div>

            <nav>...</nav>
        </header>        
    )
};
export default toolbar;
